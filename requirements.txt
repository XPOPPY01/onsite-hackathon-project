# Zoro AI - Conflict Resolution Engine
# Core engine depends only on the Python standard library.
# The packages below power the optional local API, tests and exports.

fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
python-multipart>=0.0.9      # CSV file uploads on POST /events
websockets>=12.0             # bonus: real-time event streaming

# --- test / dev ---
pytest>=8.0
httpx>=0.27                  # FastAPI TestClient transport

# --- optional: Excel / Power BI export (bonus) ---
openpyxl>=3.1
